import { NumericFormatDirectiveDirective } from './numeric-format-directive.directive';

describe('NumericFormatDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericFormatDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
